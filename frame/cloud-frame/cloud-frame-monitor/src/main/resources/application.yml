logging:
  level:
    org.springframework.cloud.netflix.zuul.filters.post.SendErrorFilter: error

#server:
#  port: 8040

#turbine:
#  stream:
#    port: 8041
#
eureka:
  instance:
    leaseRenewalIntervalInSeconds: 10
    health-check-url-path: /actuator/health
    hostname: registry
    prefer-ip-address: true
#    metadata-map:
#      user.name: ${security.user.name}
#      user.password: ${security.user.password}
  client:
    registryFetchIntervalSeconds: 5
    service-url:
      defaultZone: http://user:${REGISTRY_SERVER_PASSWORD:password}@registry:8761/eureka/

management:
  server:
    port:8044
#  endpoints:
#    web:
#      exposure:
#        include: "*"  #<2>
#  endpoint:
#    health:
#      show-details: ALWAYS

#spring:
#  rabbitmq:
#    host: rabbitmq
#  boot:
#    admin:
#      routes:
#        endpoints: env,metrics,trace,dump,jolokia,info,configprops,trace,logfile,refresh,flyway,liquibase,heapdump,loggers,auditevents,fallback.stream
#      turbine:
#        clusters: default
#        location: http://monitor:${turbine.stream.port}